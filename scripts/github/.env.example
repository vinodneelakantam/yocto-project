# Copy to .env and fill values before running helper scripts.
GH_REPO=owner/repo
VPS_HOST=your.vps.host
VPS_USER=yocto-ci
VPS_PORT=22
VPS_BUILD_ROOT=~/yocto-project
# Do not commit secrets. Export VPS_SSH_KEY from a secure source.
